using Anvil.API;

namespace NWN.Systems
{
  public static partial class SpellUtils
  {
    /*public static int GetCasterClassSpellSlots(NwCreature caster, NwClass casterClass)
    {
      switch(casterClass.Id)
      {
        case CustomClass.Cleric:
        case CustomClass.Wizard:
        case CustomClass.Bard:
        case CustomClass.Druid: return caster.GetClassInfo(casterClass).Level > ;


      }
      //GetCasterSpellAbilityModifier(caster, spell)
    }*/
  }
}
